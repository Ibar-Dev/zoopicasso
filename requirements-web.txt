# Dependencias exclusivas del servidor web FastAPI.
# No incluye: flet (desktop), python-escpos/libusb/pyusb (impresora USB),
# pywin32 (Windows), requests (solo agente poll_and_print.py).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
itsdangerous>=2.2.0
openpyxl>=3.1.5
pydantic>=2.0
